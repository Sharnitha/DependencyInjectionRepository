apiVersion: apps/v1
kind: Deployment
metadata:
  name: aks-demo
spec:
  selector:
    matchLabels:
      app: aks-demo
  template:
    metadata:
      labels:
        app: aks-demo
    spec:
      containers:
      - name: aks-demo-container
        image: githubcipractice.azurecr.io/demoenv:8766479298
        ports:
        - containerPort: 80
