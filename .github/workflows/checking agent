name: echo fiunction to execute
on:
  push:
    branches:
    - feature
jobs:
  Check:
    runs-on: self-hosted
    - name
      run: echo "Hello world"
    - name: listing files and directory
      run: |
        ls 
        pwd
